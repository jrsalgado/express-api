db.todos.insert({
  title:'install Docker tools', 
  desc:"Dont install Docker form Mac"
})
db.todos.insert({
  title:'Run container from oficial images', 
  desc:"Use docker-run branch and connect to data base manually"
})
db.todos.insert({
  title:'Run containers using docker run command and Link mongo and redis containers to nodejs', 
  desc:"Use docker-run-link branch and connect to data base auto"
})
db.todos.insert({
  title:'Build and image and Run mongo container using a Docker file',
  desc:"Use docker-flie-init branch to build an image and run a container, then link the other containers, now you can push this image to your dockerhub account "
})
db.todos.insert({
  title:'Now you can use docker-compose', 
  desc:"Well i guess we could skip the previous steps, but where is the fun in that?, use master branch btw"
})
